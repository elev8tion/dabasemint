# dabasemint Current State

Last updated: 2026-07-14 (UX overhaul)

`dabasemint` is now a functional local-first visual workshop for `/forge` toolchests with:

- Vite + vanilla JS web UI
- Optional Tauri desktop wrapper
- Native/browser toolchest loading
- Composition canvas and real-project export flow
- Agent proxy + provider status paths
- CLI commands for registry and assay workflows
- Graphify project graph in `graphify-out/`

## Current source of truth

- `README.md` — how to run and operate the project
- `docs/CURRENT-STATE.md` — this file: living snapshot + verification baseline
- `docs/GLOSSARY.md` — terminology (mirrors the in-app `TERMS` tooltips)
- `src/main.js` — UI orchestration (tabs, wizard, tour, search, compare, agents)
- `index.html` — app shell (tab bar, Start here, section anchors)
- `src/styles.css` — dark-neon theme + light/dark tokens
- `src/toolchest-loader.js` — disk ingestion
- `src/agent-provider.mjs` + `src/agent-touchpoints.mjs` — agent layer
- `src-tauri/` — desktop wrapper and sidecar management
- `graphify-out/GRAPH_REPORT.md` — architecture map generated by Graphify

## Archived docs

Historical planning/session docs were moved to `docs/archive/2026-07-14-doc-cleanup/`
because they describe completed or superseded work (the original 6-phase strategic
plan is fully implemented, including the 2026-07-14 UX overhaul that succeeded it).
Keep them for audit context only — they are not current implementation guidance.

## Verification baseline

Use:

```bash
npm run verify   # 14 health + structural checks
npm test         # 18 e2e checks (agent layer + inline-handler wiring)
npm run build    # production bundle
```

## UX overhaul (2026-07-14)

The web UI was restructured around five primary tabs and a single main workflow:

- **Tabs:** Library · Anatomy · Blueprint · Agents · Exports, plus an **Advanced**
  menu (Snapshots, Trade Routes, Registry IO, Provider Settings).
- **First-run onboarding:** a guided tour auto-shows on a fresh localStorage;
  replay it any time via the **Tour** button. A **Start here** panel sits above
  the library until dismissed.
- **Main workflow:** **✨ Create a project from toolchests** opens a 5-step
  wizard (pick toolchests → choose modules → review conflicts → generate docs →
  export).
- **Empty states:** every tab shows an actionable empty card with direct
  buttons when there is nothing to show yet.
- **Glossary tooltips:** hover any underlined term (toolchest, blueprint, mint,
  assay, trade routes) for an inline definition. Source: `docs/GLOSSARY.md` ↔
  the `TERMS` object in `src/main.js`.
- **Search:** natural-language-ish (“auth modules”), multi-field (name, README,
  contracts, tags), filter chips (source, role, contracts-only, min health),
  and saveable searches.
- **Compare:** tick checkboxes in Anatomy to compare up to 4 modules
  side-by-side (reusability, best-for, contracts, transplant warnings).
- **Agent Actions panel:** a button grid (Analyze, Recommend, Explain, Find
  missing, Generate docs, Audit) plus a metadata strip (last run, confidence,
  time, provider) and an expandable raw result.
- **Export preview:** before generating, preview the folder tree, files,
  naming conflicts, and missing contracts/READMEs.
- **Action feedback:** success cards (not just toasts) with links and **Undo**
  for destructive actions (remove toolchest, clear blueprint).
- **File status:** per-toolchest badges (Connected / Missing / Changed /
  Permission) polled on focus + every 60s.
- **Next-steps:** contextual “what to do next” card at the bottom of the active
  tab, driven by current state.
- **Theming:** light + dark via CSS custom properties
  (`prefers-color-scheme` and `[data-theme]`).

All new state persists to `localStorage` and no existing `window.*` API was
removed (only added). `npm run verify` (14 checks) and `npm test` (18 checks)
stay green. See `docs/GLOSSARY.md` for term definitions.
